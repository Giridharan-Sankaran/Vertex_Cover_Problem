//This hpp file is created to hold the function declarations required for finding the shortest path of a undirected graph.

#include<string>
#include<vector>

class Graph
{
    public:

    std::vector<int> computevertexcover(int mid, int vertex, std::vector<int> edgevector);  // This function is used to split the edges from the given input into the form of (u,v).


};